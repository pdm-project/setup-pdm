on: [push]

name: Test Action

jobs:
  Testing:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        python-version: ["3.10"]
        os: ["windows-latest"]
    name: Test the action
    steps:
      - uses: actions/checkout@v2
      - name: Setup PDM
        uses: ./
        with:
          python-version: ${{ matrix.python-version }}
          ref: ca2b0df71e27f407cc10a6915d5b3d60cf3bec75

      - name: Install dependencies
        run: pdm install -v

      - name: Verify python version
        run: python test.py
        env:
          PYTHON_VERSION: ${{ matrix.python-version }}
